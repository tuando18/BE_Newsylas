<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Login</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Đường dẫn đến file CSS của Bootstrap -->
    <link rel='stylesheet' href='/stylesheets/login.css' />
    <!-- Đường dẫn đến file CSS của bạn nếu có -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="/socket.io/socket.io.js"></script>
</head>

<body>
    <div class="container-fluid">
        <div class="row auth-wrapper">
            <div class="col-lg-6 d-none d-lg-block left-section"
                style="background-image: url('https://images.unsplash.com/photo-1497215728101-856f4ea42174')">
                <div class="overlay"></div>
                <div class="content">
                    <h1>Welcome Back!</h1>
                    <p>Login to access your dashboard</p>
                </div>
            </div>
            <div class="col-lg-6 col-12 right-section">
                <div class="login-form">
                    <div class="logo mb-4">🎯 AdminPanel</div>
                    <h2 class="mb-4">Login to Dashboard</h2>
                    <form class="pt-3" id="loginForm" enctype="multipart/form-data"> <!-- Updated form action -->
                        <div class="form-group mb-3">
                            <label for="username" class="form-label">Username</label>
                            <input type="text" class="form-control" id="username" name="username" required
                                placeholder="Enter your username">
                            <div class="invalid-feedback">Please enter your username</div>
                        </div>
                        <div class="form-group mb-4">
                            <label for="password" class="form-label">Password</label>
                            <div class="input-group">
                                <input type="password" class="form-control" id="password" name="password" required placeholder="Enter your password">
                                <button class="btn btn-outline-secondary" type="button" id="togglePassword">👁️</button>
                            </div>
                            <div class="invalid-feedback">Password must be at least 8 characters</div>
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <span class="spinner-border spinner-border-sm d-none" role="status"
                                    aria-hidden="true"></span>
                                Login
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal -->
    <div class="modal fade" id="loginErrorModal" tabindex="-1" aria-labelledby="loginErrorModalLabel"
        aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="loginErrorModalLabel">Login Error</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p id="errorMessage">Invalid username or password.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/javascripts/login.js"></script> 
    {{!--
    <script>
        $(document).ready(function () {
            $('#loginForm').on('submit', function (event) {
                event.preventDefault(); // Prevent default form submission

                // Get form data
                var formData = {
                    username: $('#username').val(),
                    password: $('#password').val()
                };

                // Disable the submit button, show loading indicator, and change button text
                $('#buttonLogin').prop('disabled', true);
                $('#spinner').show(); // Show spinner
                $('#buttonText').hide(); // Change button text to indicate processing

                // Send the form data to the server via AJAX
                $.ajax({
                    url: '/login', // URL of your login route
                    type: 'POST',
                    data: formData,
                    success: function (response) {
                        // Set a minimum display time for the spinner
                        setTimeout(function () {
                            // Re-enable the submit button and hide loading indicator
                            $('#buttonLogin').prop('disabled', false);
                            $('#spinner').hide(); // Hide spinner
                            $('#buttonText').show()
                            if (response.success) {
                                // If login is successful, redirect to homepage
                                window.location.href = '/';
                            } else {
                                // If login fails, display the error message in the toast
                                $('#errorMessage').text(response.message);
                            }
                        }, 1000); // Minimum display time of 1000 ms (1 second)
                    },
                    error: function () {
                        // Set a minimum display time for the spinner
                        setTimeout(function () {
                            // Re-enable the submit button and hide loading indicator
                            $('#buttonLogin').prop('disabled', false);
                            $('#spinner').hide(); // Hide spinner
                            $('#buttonText').show()

                            // Handle any unexpected errors from the server
                            $('#toastBody').text('An unexpected error occurred. Please try again.');
                            $('#errorToast').toast({
                                delay: 3000 // Show for 3 seconds
                            });
                            $('#errorToast').toast('show');
                        }, 1000); // Minimum display time of 1000 ms (1 second)
                    }
                });
            });
        });

    </script> --}}
</body>

</html>